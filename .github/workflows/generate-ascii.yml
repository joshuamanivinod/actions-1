name: generate ascii

on: push

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout repo
          uses: actions/checkout@v4

        - name: list repo file
          run: ls -ltra

        - name: executing shell script
          run: |
           chmod +x ./ascii-script.sh
           ./ascii-script.sh

